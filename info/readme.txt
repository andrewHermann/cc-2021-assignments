Participants:

Suada Abukar
Julian Weyermann
Andrew Hermann


Instructions:

Before running docker-compose, adapt the path to the /misc folder in the project folder so that the Database is loaded correctly
run ```docker-compose up -d``` from the project root, and everything should be running. 